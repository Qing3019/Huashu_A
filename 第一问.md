# 问题一：基于精确截断 GJK 与连通图的微构体导通判定

## 主要符号说明

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $i,j$ | 导体片段或附件数据行编号 | 正整数 |
| $A_i,B_i$ | 附件第 $i$ 行给出的两个轴线端点 | nm |
| $u_i$ | 第 $i$ 个圆柱的轴向单位向量 | $\|u_i\|=1$ |
| $t_i^{\min},t_i^{\max}$ | 轴线与边界盒相交区间在 $u_i$ 方向上的投影端点 | nm |
| $\Omega$ | 当前微构体的轴对齐边界盒 | nm |
| $r$ | 圆柱半径 | $30$ nm |
| $C_i$ | 根据第 $i$ 行轴线构造的辅助有限圆柱 | — |
| $P_i$ | 主单元内的实际导体片段，$P_i=C_i\cap\Omega$ | nm |
| $c_i,h_i$ | 辅助圆柱的中心和半长 | nm |
| $e_{i1},e_{i2}$ | 与 $u_i$ 正交的两个径向单位向量 | 无量纲 |
| $z=(z_0,z_1,z_2)^\mathsf T$ | 圆柱局部无量纲坐标 | 无量纲 |
| $M_i$ | 局部坐标到世界坐标的线性映射矩阵 | nm |
| $D_i z\le q_i$ | 端盖约束和边界盒约束的局部线性形式 | 无量纲 |
| $d$ | 支持映射的查询方向 | 任意非零向量 |
| $s_P(d)$ | 凸集 $P$ 沿方向 $d$ 的支持点 | nm |
| $\mathcal M_{ij}$ | 闵可夫斯基差 $P_i-P_j$ | nm |
| $v_k$ | GJK 第 $k$ 次迭代中单纯形到原点的最近点 | nm |
| $\lambda_m$ | 最近点关于单纯形顶点的重心坐标 | $\sum_m\lambda_m=1$ |
| $\underline{x}_i,\overline{x}_i$ | 片段 $P_i$ 的 AABB 下、上界向量 | nm |
| $g_k$ | 两片段 AABB 在第 $k$ 个坐标方向上的区间间距 | nm |
| $\delta$ | 导体之间的导通距离阈值 | $1.8$ nm |
| $\delta_a$ | 为阈值审计设置的安全粗筛距离 | $2.5$ nm |
| $L,R$ | 左、右带电面对应的图节点 | — |
| $G=(V,E)$ | 导体片段及电极构成的无向连通图 | — |

## 1. 问题分析

问题一要求根据附件给出的介质 A 坐标，判断三个微构体的左右带电面之间是否存在完整导电通路。其核心包含两个层次：首先计算任意两个导体片段在微构体内部的最短欧氏距离；其次将满足导通条件的片段构造成无向图，判断左右电极是否属于同一连通分量。

由于附件中部分圆柱被微构体边界截断，若直接把每行数据当作普通有限圆柱，会错误地把边界交点处理为垂直于轴线的圆形端盖。为保持边界切面形状，本模型先构造圆柱与边界盒的精确交集，再利用优化版 GJK 算法计算截断片段间距离，最后用并查集判断整体导通性，并用 BFS 提取一条代表性通路。

## 2. 模型假设

在题目基本假设的基础上，作如下约定：

1. 附件每一行表示一个独立导体片段，并对应导通图中的一个节点；
2. 位于微构体内部的轴线端点视为圆柱的真实圆形端盖，位于边界面上的轴线端点视为圆柱被边界截断的位置；
3. 两导体片段在主单元内的最短欧氏距离不超过阈值 $\delta=1.8$ nm 时视为导通；
4. 垂直于 $X$ 轴的两个相对面分别视为左、右电极，其余四个面绝缘；
5. 左右电极之间存在由导体片段构成的普通连通路径时，判定微构体导通。

附件中组1、组2出现的截断坐标范围为

$$
x\in[-5000,5000],\qquad y,z\in[-500,500],
$$

组3的三个方向均为 $[-5000,5000]$。计算时采用附件所反映的实际截断边界。该数据口径与题面统一边长 10000 nm 的表述存在差异，需在论文的模型假设或误差分析中明确说明。

## 3. 截断圆柱的精确几何表示

### 3.1 实际片段的构造

设附件第 $i$ 行给出的两个轴线点为 $A_i,B_i$，其轴向单位向量为

$$
u_i=\frac{B_i-A_i}{\|B_i-A_i\|}.
$$

若端点严格位于边界盒内部，则将其视为真实圆形端盖；若端点位于边界面上，则将其视为圆柱被边界截断的位置，而不直接设置圆形端盖。为得到正确的平面切口，程序沿轴向将边界端点延伸到盒外，构造辅助有限圆柱 $C_i$，再定义实际片段为

$$
P_i=C_i\cap\Omega.
$$

具体地，先计算边界盒在轴向上的投影范围

$$
t_i^{\min}=\min_{x\in\Omega}u_i^\mathsf T(x-A_i),
\qquad
t_i^{\max}=\max_{x\in\Omega}u_i^\mathsf T(x-A_i),
$$

再将边界端的辅助轴段延伸到该范围之外，最终端面由边界平面切出。盒外辅助部分不参与距离计算，也不会被平移或生成新的导体片段。

### 3.2 局部坐标与支持映射

取径向正交基 $e_{i1},e_{i2}$，在辅助圆柱的局部坐标系中写成

$$
x=c_i+M_i z,
\qquad
M_i=[h_i u_i,\ r e_{i1},\ r e_{i2}],
$$

其中

$$
|z_0|\le1,
\qquad
z_1^2+z_2^2\le1.
$$

将端盖与边界盒约束代入局部坐标，可统一表示为 $D_i z\le q_i$。因此片段沿方向 $d$ 的支持点由下列优化问题得到：

$$
s_{P_i}(d)=c_i+M_i z_i^*,
\qquad
z_i^*=\underset{z}{\arg\max}\ (M_i^\mathsf T d)^\mathsf Tz,
$$

$$
\text{s.t.}\quad
D_i z\le q_i,
\qquad
z_1^2+z_2^2\le1.
$$

程序通过枚举端盖、边界切面和圆柱侧面的活动约束求取候选极值，并用 KKT 条件验证；退化情形采用三变量凸优化回退。该表示始终保留圆形侧面，没有将圆周离散为多边形。

## 4. 基于优化版 GJK 的片段距离

### 4.1 AABB 距离下界

在调用 GJK 前，先利用轴对齐包围盒进行安全粗筛。分别查询片段在三个坐标轴正、负方向上的支持点，可得到其精确 AABB：

$$
\operatorname{AABB}(P_i)=[\underline{x}_i,\overline{x}_i].
$$

记 $e_k$ 为第 $k$ 个笛卡尔坐标单位向量，则各坐标上下界为

$$
\overline{x}_{i,k}=e_k^\mathsf T s_{P_i}(e_k),
\qquad
\underline{x}_{i,k}=e_k^\mathsf T s_{P_i}(-e_k),
\qquad k=1,2,3.
$$

对两个 AABB，第 $k$ 个坐标方向上的区间间距定义为

$$
g_k=\max\left\{
0,
\underline{x}_{i,k}-\overline{x}_{j,k},
\underline{x}_{j,k}-\overline{x}_{i,k}
\right\}.
$$

若两个区间在该方向重叠，则 $g_k=0$；否则 $g_k$ 等于两个区间的距离。因此两个 AABB 的最短距离为

$$
d_{\mathrm{AABB}}(i,j)
=\sqrt{g_1^2+g_2^2+g_3^2}.
$$

由于

$$
P_i\subseteq\operatorname{AABB}(P_i),
\qquad
P_j\subseteq\operatorname{AABB}(P_j),
$$

扩大集合不会增大两集合间的最短距离，故

$$
d_{\mathrm{AABB}}(i,j)\le d(P_i,P_j).
$$

程序采用 $\delta_a=2.5$ nm 作为安全筛选阈值。若 $d_{\mathrm{AABB}}(i,j)>\delta_a$，则必有 $d(P_i,P_j)>2.5>1.8$ nm，该片段对不可能导通，可以安全排除。

### 4.2 外包圆柱的第二级下界

通过 AABB 筛选的片段对还要计算辅助圆柱 $C_i,C_j$ 的距离。由于

$$
P_i\subseteq C_i,
\qquad
P_j\subseteq C_j,
$$

故有

$$
d(C_i,C_j)\le d(P_i,P_j).
$$

辅助圆柱具有解析支持映射。对任意方向 $d$，令其径向分量为

$$
d_\perp=d-(d^\mathsf T u_i)u_i.
$$

根据 $d^\mathsf T u_i$ 的符号选择轴向端点，再沿 $d_\perp$ 选择圆周极点，可得

$$
s_{C_i}(d)
=c_i+\operatorname{sgn}(d^\mathsf T u_i)h_i u_i
+r\frac{d_\perp}{\|d_\perp\|},
$$

其中 $d_\perp=0$ 时径向项取0。利用该支持映射运行普通有限圆柱 GJK；若 $d(C_i,C_j)>\delta_a$，也可安全排除。只有同时通过 AABB 和辅助圆柱两级下界筛选的片段对，才调用截断片段的精确支持映射。

### 4.3 闵可夫斯基差与 GJK 主迭代

对两个凸片段 $P_i,P_j$，定义闵可夫斯基差

$$
\mathcal M_{ij}
=P_i-P_j
=\{x-y:x\in P_i,\ y\in P_j\}.
$$

两片段的最短距离等于闵可夫斯基差到原点的距离：

$$
d(P_i,P_j)=\min_{w\in\mathcal M_{ij}}\|w\|.
$$

闵可夫斯基差沿方向 $d$ 的支持点可由两个片段的支持映射直接得到：

$$
s_{\mathcal M_{ij}}(d)
=s_{P_i}(d)-s_{P_j}(-d).
$$

令 $S_k$ 为第 $k$ 次迭代保留的单纯形，$v_k$ 为 $\operatorname{conv}(S_k)$ 中距原点最近的点。初始方向取两片段中心之差；若两中心重合，则取任一非零坐标轴方向。第 $k$ 次迭代沿 $-v_k$ 查询新支持点

$$
w_k=s_{\mathcal M_{ij}}(-v_k)
=s_{P_i}(-v_k)-s_{P_j}(v_k),
$$

并构造候选单纯形

$$
\widetilde S_{k+1}=S_k\cup\{w_k\}.
$$

随后在 $\operatorname{conv}(\widetilde S_{k+1})$ 中求距原点最近的点，删除不可能参与最近点凸组合的顶点，得到新的 $S_{k+1}$ 与 $v_{k+1}$。三维空间中的单纯形最多包含4个顶点。

程序同时保存每个闵可夫斯基支持点的来源点对 $w_r=x_r-y_r$。若最近点的重心坐标为 $\lambda_r$，则两个片段上的对应最近点为

$$
x^*=\sum_r\lambda_r x_r,
\qquad
y^*=\sum_r\lambda_r y_r,
\qquad
v=x^*-y^*.
$$

因此 GJK 不仅能够返回距离 $\|v\|$，还可以恢复实现该距离的两个几何点。

### 4.4 Signed Volumes 单纯形更新

在单纯形更新部分，用Signed Volumes 方法及逆行优化，使用带符号长度、面积或体积计算最近点的重心坐标。设当前单纯形顶点为 $w_0,\ldots,w_m$，最近点表示为

$$
v=\sum_{r=0}^{m}\lambda_r w_r,
\qquad
\sum_{r=0}^{m}\lambda_r=1.
$$

若所有 $\lambda_r\ge0$，则仿射投影位于当前单纯形内部；若存在负权重，则原点位于相应面的外侧，需要转到相应低维子单纯形上继续计算。

**（1）线段单纯形。** 对端点 $a,b$，令 $e=b-a$，原点在线段所在直线上的投影参数为

$$
t=-\frac{a^\mathsf T e}{e^\mathsf T e}.
$$

若 $0<t<1$，则最近点为

$$
v=(1-t)a+tb,
$$

重心坐标为 $(1-t,t)$；若 $t\le0$ 或 $t\ge1$，最近点分别退化为端点 $a$ 或 $b$。

**（2）三角形单纯形。** 对顶点 $a,b,c$，三角形法向量为

$$
n=(b-a)\times(c-a).
$$

原点到三角形所在平面的正交投影为

$$
p=\frac{a^\mathsf T n}{n^\mathsf T n}n.
$$

将三角形投影到数值最稳定的二维坐标平面，利用 Cramer 法则计算

$$
p=\lambda_0a+\lambda_1b+\lambda_2c,
\qquad
\lambda_0+\lambda_1+\lambda_2=1.
$$

三个重心坐标的分子分别对应三个有向子三角形的面积。若它们均非负，则 $p$ 位于三角形内部；否则在相应边或顶点上继续计算最近点。

**（3）四面体单纯形。** 对顶点 $a,b,c,d$，构造齐次矩阵

$$
T=
\begin{bmatrix}
a&b&c&d\\
1&1&1&1
\end{bmatrix}.
$$

原点的重心坐标满足

$$
T
\begin{bmatrix}
\lambda_0\\
\lambda_1\\
\lambda_2\\
\lambda_3
\end{bmatrix}
=
\begin{bmatrix}
0\\0\\0\\1
\end{bmatrix}.
$$

由 Cramer 法则，$\lambda_r$ 等于相应有向子四面体体积与总体积之比。若四个权重均非负，则原点位于四面体内部，即

$$
0\in\mathcal M_{ij},
$$

从而两片段相交，距离为0；若存在负权重，则在原点可能投影到的三角形面上继续求最近点。

当线段长度、三角形面积或四面体体积接近0时，直接使用有向体积可能出现数值不稳定。程序枚举其可行低维子面，对原点作仿射投影，保留重心坐标非负的候选，并选择距离最小者作为退化回退结果。

### 4.5 收敛与导通判定

设当前最近点为 $v_k$，新支持点为 $w_k$。支持平面给出的改进间隙为

$$
\gamma_k=\|v_k\|^2-v_k^\mathsf T w_k.
$$

当新支持点与历史支持点重复，或满足

$$
\gamma_k
\le\varepsilon_{\mathrm{rel}}^2
\max\{\|v_k\|^2,1\},
\qquad
\varepsilon_{\mathrm{rel}}=10^{-6},
$$

则认为当前单纯形已无法显著逼近原点，返回

$$
d(P_i,P_j)=\|v_k\|.
$$

若单纯形成为包含原点的四面体，或 $\|v_k\|^2$ 小于接触容差，则返回距离0。程序设置最大迭代次数为1024，并记录每次精确 GJK 是否正常收敛。最终只依据截断片段 GJK 的结果建立导通边：

$$
d(P_i,P_j)\le\delta=1.8\text{ nm}.
$$

完整距离计算流程为

$$
\boxed{
\text{AABB下界}
\longrightarrow
\text{外包圆柱GJK下界}
\longrightarrow
\text{截断片段精确GJK}
\longrightarrow
1.8\text{ nm阈值判定}
}.
$$

## 5. 导通图与判定方法

构造无向图

$$
G=(V,E),\qquad V=\{L,R,P_1,\ldots,P_n\},
$$

其中 $L,R$ 分别表示左右电极。对任意两个导体片段定义

$$
a_{ij}=
\begin{cases}
1,&d(P_i,P_j)\le\delta,\\
0,&d(P_i,P_j)>\delta.
\end{cases}
$$

设片段在 $X$ 方向的最小、最大坐标为 $x_i^{\min},x_i^{\max}$，则其到左右电极的距离为

$$
d_L(P_i)=\max\{0,x_i^{\min}+5000\},
\qquad
d_R(P_i)=\max\{0,5000-x_i^{\max}\}.
$$

当 $d_L(P_i)\le\delta$ 时加入边 $(L,P_i)$，当 $d_R(P_i)\le\delta$ 时加入边 $(P_i,R)$。随后将全部导通边加入并查集。若

$$
\operatorname{Find}(L)=\operatorname{Find}(R),
$$

则微构体导通。BFS 仅在导通后用于提取一条展示路径，不参与导通判定。

算法流程为：

    输入：轴线端点、半径 r、阈值 δ、边界盒 Ω
    输出：微构体是否导通及一条代表性路径

    1. 每行构造辅助圆柱，并与 Ω 求交得到独立片段 Pi；
    2. 将左右电极及所有片段加入并查集；
    3. 根据片段到左右面的距离加入电极边；
    4. 枚举片段对，用 AABB 和辅助圆柱距离进行安全筛选；
    5. 对候选对用精确截断 GJK 求最短距离；
    6. 若距离不超过 δ，则合并两个片段节点；
    7. 判断左右电极是否属于同一并查集；
    8. 若导通，则用 BFS 提取一条展示路径。

## 6. 计算结果

| 微构体 | 独立片段数 | 距离不超过1.8 nm的片段对数 | 接左电极片段数 | 接右电极片段数 | 导通结果 |
|---|---:|---:|---:|---:|---|
| 组1 | 12 | 2 | 3 | 4 | 不导通 |
| 组2 | 49 | 27 | 11 | 11 | 导通 |
| 组3 | 535 | 165 | 92 | 90 | 导通 |

### 6.1 组1不导通

组1中仅存在两条片段连接边：

$$
(1,4),\qquad(9,10),
$$

且两对片段的距离均为0。接触左电极的片段为 $\{1,2,3\}$，接触右电极的片段为 $\{8,9,10,12\}$。没有任何普通连通分量同时包含左、右电极片段，因此组1不导通。

### 6.2 组2导通

组2存在完整通路

$$
L\rightarrow P_2\rightarrow P_{12}\rightarrow P_{24}
\rightarrow P_{39}\rightarrow R.
$$

$P_2$ 接触左电极，$P_{39}$ 接触右电极，路径中的三个片段连接距离均为0，因此组2导通。

### 6.3 组3导通

组3存在完整通路

$$
L\rightarrow P_{63}\rightarrow P_{264}\rightarrow P_{216}
\rightarrow P_{351}\rightarrow R.
$$

$P_{63}$ 接触左电极，$P_{351}$ 接触右电极，相邻片段距离均为0，因此组3导通。

综上，

$$
\boxed{\text{组1不导通，组2导通，组3导通。}}
$$

## 7. 数值稳定性与结果验证

最接近导通阈值的两侧距离均出现在组3：

$$
d(P_{206},P_{303})=1.7786710871\text{ nm}<1.8\text{ nm},
$$

$$
d(P_{165},P_{174})=1.8439571373\text{ nm}>1.8\text{ nm}.
$$

二者与阈值的间隔分别约为 $0.02133$ nm 和 $0.04396$ nm。对这两组临界片段另采用六变量约束优化直接求最短距离，与 GJK 结果的差异分别约为

$$
2.43\times10^{-13}\text{ nm},\qquad
1.53\times10^{-12}\text{ nm}.
$$

全部精确 GJK 调用均收敛，两种距离算法的阈值判断完全一致，说明导通结论对数值误差稳定。

## 8. 模型局限与论文表述边界

1. 结论建立在“附件每一行是独立导体片段”的解释上；若改为周期边界下的同源片段重组或周期绕行，图模型及结果可能改变；
2. 边界端点被解释为边界截断点，而非真实圆形端盖。附件未提供切割前父圆柱编号，无法完全恢复原始圆柱；
3. 组1、组2的 $Y/Z$ 截断坐标与题面统一边长的表述不一致，本文按附件数据口径处理，建议补充统一采用 $\pm5000$ nm 边界时的敏感性分析；
4. 文中的“精确”指几何模型没有多边形离散，数值求解仍使用双精度浮点数。

## 9. 本问结论

本文建立了“精确截断几何—GJK最短距离—导通图—并查集判定”的计算模型。该模型能够处理被边界斜切的圆柱片段，并避免圆周多边形离散造成的几何误差。第一组虽然分别存在接触左右电极的导体，但这些导体不属于同一连通分量，因此不导通；第二组和第三组均存在完整的左右导电路径，因此导通。
