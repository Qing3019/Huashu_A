# A题 可视化图表说明与使用指南

---

## 统一配色方案

| 颜色 | 色号 | 用途 |
|------|------|------|
| 蓝色 | `#2471a3` | 主曲线、柱状图、左电极 |
| 红色 | `#cb4335` | 目标线、最优解标记、导通路径 |
| 绿色 | `#1e8449` | 认证通过、经验阈值、稳健方案 |
| 橙色 | `#e67e22` | 置信区间高亮、右分量、次级前沿 |
| 紫色 | `#7d3c98` | 收敛曲线 |
| 灰色 | `#95a5a6` / `#5d6d7e` | 背景、非活跃元素 |

---

## Q1：三维导电路径图（3 张）

**文件**：`问题1_并查集_GJK_BFS.ipynb` → 替换 `plot_sheet` 函数及调用

**图纸含义**：

| 元素 | 含义 |
|------|------|
| 灰色细线 | 微构体中所有介质 A 圆柱轴线（背景） |
| 红色粗线 | BFS 搜索到的导通路径，标注了片段编号 |
| 蓝色半透明面 (X=-5000) | 左带电面 |
| 红色半透明面 (X=+5000) | 右带电面 |
| 蓝色线（仅组1） | 与左电极连通的片段分量 |
| 橙色线（仅组1） | 与右电极连通的片段分量 |

**横轴 X**：微构体长度方向（垂直于带电面），范围约 ±5000 nm
**纵轴 Y、Z**：截面方向，范围随微构体尺寸变化

**论文用途**：展示微构体的空间结构，直观说明导通/不导通结论的空间几何原因。

---

## Q2：概率收敛图 + 柱状估计图（1 张）

**文件**：`问题2_导通概率.ipynb` → 替换 cell `c21788cd`

**左子图——累计导通概率收敛**：

| 元素 | 含义 |
|------|------|
| 四条彩色曲线 | 四个体积分数各自的累计导通概率随试验次数的变化 |
| 半透明色带 | 逐点 95% Wilson 置信区间 |

- **横轴**：累计试验次数（1→2500）
- **纵轴**：累计导通概率（0→1）
- **用途**：验证蒙特卡洛已充分收敛，展示估计的稳定性

**右子图——最终概率估计柱状图**：

| 元素 | 含义 |
|------|------|
| 四根彩色柱 | 四个体积分数下的导通概率点估计 |
| 黑色误差线 | 95% Wilson 置信区间 |
| 柱上方数字 | 点估计值（保留四位小数） |

- **横轴**：四个目标体积分数（分类变量）
- **纵轴**：导通概率（0→1）
- **用途**：论文核心结果展示

---

## Q3：首次导通分析四合一图（1 张）

**文件**：`问题3_最低填充量.ipynb` → 替换 cell `49c58a07`

**左上——导通概率 CDF**：

- **横轴**：名义体积分数（%），范围约 0.65%～1.0%
- **纵轴**：经验导通概率
- 红色虚线：目标 90%；绿色虚线：经验阈值位置
- **用途**：展示导通概率随填充量的 S 形增长趋势，标注 90% 阈值位置

**右上——90% 附近局部放大**：

- **横轴**：完整原圆柱数量 N，范围 $N_{90}\pm20$
- **纵轴**：经验导通概率
- 橙色半透明带：候选舍入档（所有舍入至 0.86% 的 N 值区间）
- **用途**：说明 0.86% 舍入档的确定逻辑

**左下——T 的分布直方图**：

- **横轴**：首次导通圆柱数 T，每 5 根一个 bin
- **纵轴**：试验频数
- **用途**：展示首次导通时间的分布形态

**右下——N90 收敛过程**：

- **横轴**：累计试验次数
- **纵轴**：经验 90% 分位数对应的体积分数
- **用途**：展示估计量随样本量增加而稳定的过程，验证样本量充足

---

## Q4：成本前沿图（1 张）

**文件**：`Q4/问题4_混合介质最低成本_严格GJK.ipynb` → 替换 cell `f56d2755`

| 元素 | 含义 |
|------|------|
| 蓝色实线 + 圆点 | 90% 点估计前沿——每个 N_A 处满足 90% 导通的最低成本 |
| 橙色虚线 + 圆点 | 91% 稳健筛选前沿——用 91% 阈值筛选的前沿线 |
| 红色大圆点 | 点估计最优解 (608, 5)，成本 9.0335 元 |
| 绿色大方点 | 独立验证通过的稳健方案 (611, 25)，成本 9.1116 元 |

- **横轴**：介质 A 数量 N_A
- **纵轴**：满足 90% 导通所需的最低成本（元）
- **用途**：展示成本-填充量的权衡关系，两个最优解的位置关系说明"统计保证"的成本代价极小

---

## 使用方法

每个 `.py` 文件对应一个 notebook 的可视化 cell。在对应的 notebook 中：

1. **先运行** `可视化优化_统一配色.py` 的内容（在第一个可视化 cell 前），设置全局样式
2. **替换** 原可视化 cell 的内容为对应 `Q?_可视化优化.py` 的内容
3. **重新运行** 该 cell

所有图表的配色、字体、线宽、标注均已统一优化，适合直接用于竞赛论文。
