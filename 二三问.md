# 问题二与问题三：基于蒙特卡洛模拟的导通概率估计与最低填充量优化

---

# 第一部分：问题二——i.i.d. 均匀随机模型下的导通概率估计

## 主要符号说明（问题二）

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $\Omega$ | 微构体基本单元盒 | $[-5000,5000]^3$ nm$^3$ |
| $N$ | 一次随机试验中放入的完整介质 A 圆柱总数 | 正整数 |
| $\boldsymbol c_i$ | 第 $i$ 根圆柱的轴中点 | nm |
| $\boldsymbol u_i$ | 第 $i$ 根圆柱的轴向单位向量 | $\|\boldsymbol u_i\|=1$ |
| $\phi$ | 名义体积分数 | 无量纲，$0<\phi\le 0.01$ |
| $V_{\text{cyl}}$ | 单根完整圆柱体积 | $\pi r^2 L$ nm$^3$ |
| $V_{\text{box}}$ | 基本单元盒体积 | $10000^3$ nm$^3$ |
| $r$ | 圆柱半径 | $30$ nm |
| $L$ | 圆柱长度 | $5000$ nm |
| $\delta$ | 导通距离阈值 | $1.8$ nm |
| $\varepsilon_{\text{num}}$ | 数值容差 | $10^{-5}$ nm |
| $\delta_{\text{contact}}$ | 含容差的接触限 | $\delta+\varepsilon_{\text{num}}$ |
| $F_{i,k}$ | 第 $i$ 根父圆柱的第 $k$ 个周期裁剪碎片 | — |
| $M$ | 蒙特卡洛独立试验次数 | $2500$ |
| $\hat p_N$ | $N$ 根圆柱时的经验导通概率 | $K/M$ |
| $z$ | 标准正态分位数 | $1.959964$（95%） |

## 1. 问题分析

问题一给出了三组确定性的介质 A 填充构型及其导通判定。问题二则要求在统一概率模型下，定量评估介质 A 的填充量与微构体导通概率之间的关系。

题目仅说明介质 A 可以处于"任意位置、任意方向"，没有指定概率分布。为使导通概率具有确定的频率学含义，本模型采用独立均匀随机假设。具体考察四种名义体积分数——$0.5\%$、$0.6\%$、$0.7\%$ 和 $1.0\%$——对应的导通概率，并给出 95% Wilson 置信区间。

## 2. 模型假设

在问题一基本假设的基础上，问题二增加如下约定：

1. **位置独立性**：不同圆柱的轴中点 $\boldsymbol c_i$ 在 $[-5000,5000)^3$ 内相互独立且均匀分布，即 $\boldsymbol c_i\overset{\mathrm{iid}}{\sim}U([-5000,5000)^3)$；
2. **方向各向同性**：不同圆柱的轴向单位向量 $\boldsymbol u_i$ 在单位球面 $S^2$ 上相互独立且均匀分布，代码用三维标准正态向量归一化实现；
3. **跨试验独立性**：不同蒙特卡洛试验之间完全独立，各自生成全新的随机构型；
4. **允许贯穿**：题目允许介质 A 相互重叠，因此不做碰撞拒绝或硬球排除；
5. **周期裁剪**：每根圆柱的轴线中点只按均匀分布生成一次，再通过周期搬运将所有有效截段放入基本单元盒；
6. **体积计算**：体积分数按完整父圆柱的名义体积计算，一根圆柱裁成多个碎片后仍只计一次；重叠部分不扣除。

## 3. 随机构型生成与周期裁剪

### 3.1 圆柱采样

对一次包含 $N$ 根圆柱的试验，生成过程为：

$$
\begin{aligned}
\boldsymbol c_i &\sim U([-5000,5000)^3), \quad i=1,\ldots,N,\\[4pt]
\boldsymbol v_i &\sim \mathcal N(\boldsymbol 0,\boldsymbol I_3), \quad
\boldsymbol u_i = \frac{\boldsymbol v_i}{\|\boldsymbol v_i\|}.
\end{aligned}
$$

轴中点 $\boldsymbol c_i$ 与方向 $\boldsymbol u_i$ 共同确定圆柱的两个端面中心：

$$
\boldsymbol p_i = \boldsymbol c_i - \frac{L}{2}\boldsymbol u_i,\qquad
\boldsymbol q_i = \boldsymbol c_i + \frac{L}{2}\boldsymbol u_i.
$$

用三维标准正态向量归一化而非球坐标极角均匀化，可保证方向在单位球面上的各向同性且避免极点的数值奇异性。

### 3.2 周期裁剪与碎片生成

对完整圆柱 $C_i$，先枚举其与基本单元盒有交的所有整数平移副本。设平移向量为 $\boldsymbol k\odot(10000,10000,10000)$，$\boldsymbol k\in\mathbb Z^3$，则定义

$$
F_{i,k} = \bigl(C_i - \boldsymbol k\odot(10000,10000,10000)\bigr) \cap [-5000,5000]^3.
$$

所有非空 $F_{i,k}$ 均作为独立导体碎片节点加入导通图。**同源碎片不因共享 `parent_id` 而自动合并**——一根圆柱跨越左右边界的两个裁剪碎片必须在盒内通过其他碎片的普通连通路径才能形成导通，不能仅凭身份跨越边界。

该命题在问题一的回归检查中已得到显式验证：一根跨右边界的圆柱产生左右两个同源碎片，若仅凭身份合并会导致虚假的左右导通，而代码中的并查集不赋予同源身份任何特殊地位，因此不会误判。

### 3.3 名义体积分数与圆柱数的换算

体积分数定义为 $N$ 根完整圆柱的名义体积之和与单元盒体积之比：

$$
\phi = \frac{N\cdot V_{\text{cyl}}}{V_{\text{box}}}
= \frac{N\cdot\pi r^2 L}{10000^3}.
$$

对四种目标体积分数，按传统四舍五入确定对应的整数圆柱数：

$$
N(\phi) = \operatorname{round}\!\left(\frac{\phi\cdot 10000^3}{\pi r^2 L}\right).
$$

| 目标体积分数 $\phi$ | 计算圆柱数 $N$ | 舍入后实际体积分数 |
|---|---:|---:|
| $0.5\%$ | $354$ | $0.500456\%$ |
| $0.6\%$ | $424$ | $0.599416\%$ |
| $0.7\%$ | $495$ | $0.699790\%$ |
| $1.0\%$ | $707$ | $0.999497\%$ |

## 4. 嵌套前缀试验与首次导通时刻（为问题三铺垫）

### 4.1 单调性与嵌套前缀

在同一个随机构型中逐根加入圆柱，每加入一根其全部裁剪碎片后重新检查并查集中左右电极是否连通。由于并查集只做合并操作而不会撤销，一旦左右电极在某根圆柱处导通，继续增加圆柱不可能使导通的图"断开"。因此导通事件对圆柱数量具有严格的单调性：

$$
N_1\le N_2 \;\Longrightarrow\; \mathbf 1_{\text{导通}}(N_1)\le\mathbf 1_{\text{导通}}(N_2).
$$

利用这一性质，可以在一次试验中同时检查多个圆柱数量档位：对所有档位使用同一组 $N_{\max}$ 根圆柱，取前 $N_1<N_2<\cdots$ 根作为"共同随机数前缀"。各档的边际分布与单独独立生成完全相同，但前缀共享降低了档位比较的随机波动。

### 4.2 首次导通圆柱数

定义

$$
T = \min\{N\in\mathbb N : \text{加入前 }N\text{ 根完整圆柱后首次导通}\}.
$$

$T$ 的分布完整刻画了导通事件对圆柱数量的依赖关系。对任意 $N$，有

$$
P_N = P(\text{导通}\mid N\text{ 根圆柱}) = P(T\le N).
$$

因此导通概率 $P_N$ 关于 $N$ 的曲线就是 $T$ 的累积分布函数。问题二对四个离散 $N$ 点的概率估计等价于对 $T$ 的 CDF 在四个点的取值估计；问题三寻找满足 $P_N\ge 0.90$ 的最小 $N$，等价于求 $T$ 总体的 $90\%$ 分位数。

## 5. 蒙特卡洛设计与可复现性

### 5.1 试验流程

单次嵌套前缀试验的流程如下：

```
输入：最大圆柱数 N_max，监控档位数组 levels
输出：各档导通布尔向量及诊断统计量

1. 用固定种子生成 N_max 根 i.i.d. 圆柱；
2. 初始化并查集，加入左电极 L 和右电极 R 两个根节点；
3. for parent_id = 0, 1, ..., N_max-1:
    a. 生成第 parent_id 根圆柱的所有周期裁剪碎片；
    b. 对每个碎片：
       - 检查是否接触左/右电极（X=-5000 / X=+5000 面），若是则与对应电极节点合并；
       - 用 AABB 和轴段距离粗筛可能接近的已有碎片；
       - 对候选对运行真实裁剪碎片 GJK，若距离 ≤ δ_contact 则合并；
    c. 若左右电极已连通：
       - 所有剩余档位标为 True，提前返回；
    d. 若当前圆柱数命中某个监控档位，记录该档导通状态；
4. 遍历结束后若未导通，返回各档布尔值。
```

### 5.2 随机数流的可复现设计

每个试验使用独立的 PCG64 伪随机数生成器，种子由两级 SeedSequence 确定：

$$
\text{SeedSequence}([\text{base\_seed},\ \text{trial\_index}]),\qquad
\text{base\_seed}=20260808.
$$

该设计保证：
- **串并行一致**：同一个 `trial_index` 在同一 `base_seed` 下总是产生完全相同的随机构型，与 `joblib` 的工作进程数无关；
- **逐试验独立**：不同 `trial_index` 产生统计上独立的随机流；
- **档位可比**：同一试验内各档共享随机前缀，档位之间的概率差异反映的是圆柱数量效应，而非随机波动。

## 6. 距离判定与连接逻辑（与问题一的异同）

问题二的碎片连接判定继承了问题一的整套几何管线，但有两点关键区别：

1. **周期性**：问题一只处理附件给出的裁切片段，不生成周期副本；问题二先生成完整圆柱再周期搬运，与基本盒求交后得到周期裁剪碎片。
2. **GJK 实现**：问题一使用了 Signed Volumes GJK，问题二使用基于 `closest_simplex` 与 KKT 条件的 GJK，但两者在数学上等价——均从闵可夫斯基差的支持映射出发，在单纯形上求原点的最近点。

共同点包括：
- 相同的 Fragment 支持函数（SLSQP 三变量原问题 + Dykstra 可行恢复）；
- 相同的三级筛选策略（AABB → 完整圆柱解析 GJK 下界 → 裁剪碎片 GJK）；
- 相同的 $1.8$ nm 阈值和 $10^{-5}$ nm 数值容差。

对数值临界情形——距离在 $1.8$ nm 附近 $10^{-4}$ nm 内的候选对——代码清空支持点缓存并增加 GJK 迭代次数到 500 次，仍无法收敛则携带试验编号中止，确保不将未决情形静默记为不导通。

## 7. 概率估计与 Wilson 置信区间

### 7.1 点估计

设 $M=2500$ 次独立试验中有 $K$ 次在 $N$ 根圆柱下导通，则经验导通概率为

$$
\hat p_N = \frac{K}{M}.
$$

该估计量的标准误（代入样本比例）为

$$
\operatorname{SE}(\hat p_N) = \sqrt{\frac{\hat p_N(1-\hat p_N)}{M}}.
$$

在最不利情形 $p=0.5$ 下，2500 次二项试验的 95% 正态近似半宽约为 $1.96$ 个百分点。

### 7.2 Wilson 置信区间

为避免 $p$ 接近 $0$ 或 $1$ 时正态近似的覆盖率失真，采用 Wilson 区间：

$$
\frac{\hat p + z^2/(2M) \pm z\sqrt{\hat p(1-\hat p)/M + z^2/(4M^2)}}{1+z^2/M},
\qquad z=1.959964.
$$

各档的 2500 个布尔值跨试验是 i.i.d. Bernoulli 样本，因此上式对各档的边际概率给出有效的 95% 置信区间。四档共享共同随机数前缀，区间之间具有相关性，不宣称四档同时覆盖率为 95%。

## 8. 问题二计算结果

正式计算固定 base_seed=20260808，2500 次独立嵌套前缀试验，每次生成 707 根圆柱并分别记录四档导通状态。

| 目标体积分数 | 圆柱数 $N$ | 导通次数 | 经验概率 $\hat p$ | 标准误 | Wilson 95% 下限 | Wilson 95% 上限 | 区间半宽 |
|---|---:|---:|---:|---:|---:|---:|---:|
| $0.5\%$ | $354$ | $205$ | $0.0820$ | $0.0055$ | $0.0719$ | $0.0934$ | $0.0107$ |
| $0.6\%$ | $424$ | $563$ | $0.2252$ | $0.0084$ | $0.2093$ | $0.2420$ | $0.0163$ |
| $0.7\%$ | $495$ | $1210$ | $0.4840$ | $0.0100$ | $0.4644$ | $0.5036$ | $0.0196$ |
| $1.0\%$ | $707$ | $2488$ | $0.9952$ | $0.0014$ | $0.9916$ | $0.9973$ | $0.0029$ |

### 8.1 数值诊断

| 诊断量 | 每次试验平均 | 2500 次合计 | 单次最大 |
|---|---|---|---|
| 碎片数 | $875.3$ | $2\,188\,262$ | $1319$ |
| AABB 候选对 | $16\,773.8$ | $41\,934\,590$ | $35\,382$ |
| 轴段粗筛通过数 | $429.1$ | $1\,072\,693$ | $970$ |
| 侧面见证连接数 | $247.5$ | $618\,830$ | $470$ |
| 完整圆柱 GJK 调用 | $82.5$ | $206\,207$ | $227$ |
| 裁剪碎片 GJK 调用 | $68.5$ | $171\,307$ | $194$ |
| 支持点优化调用 | $874.7$ | $2\,186\,744$ | $2463$ |
| 支持点回退次数 | $9.1$ | $22\,838$ | $43$ |
| 临界加密次数 | $0.0$ | $0$ | $0$ |

全部 2500 次试验中，**临界加密重算次数为 0**，说明所有 GJK 距离判定均与阈值保持了足够间距，不存在数值模糊的连接。

## 9. 问题二结论

在圆柱轴对称中心 i.i.d. 均匀、方向 i.i.d. 各向同性的概率模型下，采用与问题一完全一致的几何管线和 $1.8$ nm 导通阈值，2500 次独立蒙特卡洛试验给出：

$$
\boxed{
\begin{aligned}
P_{0.5\%}&=0.0820\ [0.0719,0.0934],\\
P_{0.6\%}&=0.2252\ [0.2093,0.2420],\\
P_{0.7\%}&=0.4840\ [0.4644,0.5036],\\
P_{1.0\%}&=0.9952\ [0.9916,0.9973].
\end{aligned}}
$$

导通概率随体积分数的增长呈现典型的 S 形曲线特征：在 $0.5\%\sim0.7\%$ 区间快速上升，到 $1.0\%$ 时已接近饱和。

---

# 第二部分：问题三——概率约束下的最低填充量优化

## 主要符号说明（问题三）

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $P_N$ | 放入 $N$ 根介质 A 后的导通概率 | $[0,1]$ |
| $N_{90}$ | 满足 $P_N\ge 0.90$ 的最小圆柱数 | 正整数 |
| $\phi_N$ | $N$ 根圆柱的名义体积分数 | $\%$ |
| $T$ | 首次导通所需圆柱数（随机变量） | 正整数 |
| $M$ | 蒙特卡洛独立试验次数 | $22\,500$ |
| $T_{(r)}$ | $T$ 的第 $r$ 次序统计量 | 正整数 |
| $\alpha$ | 单侧置信水平 | $0.025$（Bonferroni 校正） |
| $\Delta\phi$ | 每增加一根圆柱的名义体积分数增量 | $0.0014137\%$ |
| $N_{\max}$ | 搜索上界 | $707$ |
| $N_{\text{sentinel}}$ | 右删失哨兵值 | $708$ |

## 10. 问题分析

问题三要求在微构体导通概率不低于 $90\%$ 的前提下，求介质 A 的最低填充量，并将体积分数精确到百分号后两位。这是在问题二随机模型基础上的**概率约束逆问题**。

数学上需要求解：

$$
N_{90} = \min\{N\in\mathbb N : P_N \ge 0.90\},
\qquad
\phi_{90} = \frac{N_{90}\cdot\pi r^2 L}{10000^3}\times 100\%.
$$

直接求解需要知道 $P_N$ 的精确函数形式。借助问题二的单调性结论——导通事件对圆柱数量 $N$ 单调递增——可将逆问题转化为**首次导通圆柱数 $T$ 的 $90\%$ 分位数估计**：

$$
N_{90} = \inf\{N : P(T\le N)\ge 0.90\},
$$

即 $N_{90}$ 是随机变量 $T$ 总体的 $90\%$ 分位数。因此问题三的求解等价于：通过蒙特卡洛模拟获取 $T$ 的经验分布，再取经验 $90\%$ 分位数作为 $N_{90}$ 的点估计，并配套给出统计置信区间。

## 11. 整数优化与体积换算

名义体积分数是圆柱数的严格线性函数：

$$
\phi_N = N\cdot\Delta\phi,\qquad
\Delta\phi=\frac{\pi r^2 L}{10000^3}\times 100\%.
$$

代入 $r=30$ nm、$L=5000$ nm、单元盒边长 $10000$ nm：

$$
V_{\text{cyl}} = \pi\cdot 30^2\cdot 5000 = 4\,500\,000\pi \approx 1.413717\times 10^7\ \text{nm}^3,
$$

$$
\Delta\phi = \frac{4\,500\,000\pi}{10^{12}}\times 100\% \approx 0.0014137167\%.
$$

搜索上界取 $N_{\max}=707$，对应名义体积分数约 $1.0\%$——问题二已确认该填充量下导通概率约为 $0.9952$，远高于 $90\%$ 目标，因此对绝大多数随机构型，首次导通必然发生在 $707$ 根以内。

体积分数的两位小数舍入在整数优化完成后执行：先搜索整数 $N_{90}$，再计算 $\phi_{N_{90}}$ 并按四舍五入保留两位小数。搜索阶段不对中间体积分数作舍入。

## 12. 首次导通试验

### 12.1 首次导通时刻 $T$ 的模拟

对一次蒙特卡洛试验，逐根激活完整圆柱（每根圆柱的全部周期裁剪碎片同时加入），在每根圆柱加入后检查并查集中左右电极是否连通。记录首次导通时的父圆柱编号：

$$
T = \min\{N\in\{1,\ldots,N_{\max}\} : \text{加入前 }N\text{ 根后导通}\}.
$$

若遍历 $707$ 根仍未导通，则令 $T = N_{\max}+1 = 708$（哨兵值），表示该试验被右删失。

由于问题三需要确定首次导通的精确圆柱数，必须在**每一根**圆柱加入后都检查连通状态，而不仅仅在问题二的四个离散档位。因此单次试验调用 `simulate_nested` 时传入 `levels = np.arange(1, 708)`——所有 $707$ 个整数档位。

### 12.2 $N_{90}$ 的经验分位数估计

对 $M$ 次独立试验得到 $T_1,\ldots,T_M$，将其升序排列为 $T_{(1)}\le\cdots\le T_{(M)}$。令

$$
r_{90} = \lceil 0.90\cdot M\rceil,
$$

则经验 $90\%$ 分位数为

$$
\widehat N_{90} = T_{(r_{90})}.
$$

对应的体积分数点估计为

$$
\hat\phi_{90} = \widehat N_{90}\cdot\Delta\phi,
$$

两位小数答案由标准四舍五入得到：

$$
\phi_{\text{reported}} = \frac{\lfloor 100\cdot\hat\phi_{90}+0.5\rfloor}{100}.
$$

## 13. 统计验证

仅给出点估计不足以在竞赛论文中形成完整的统计论证。本模型设计了两套独立的统计验证程序。

### 13.1 非参数次序统计量区间

由次序统计量的概率分布，样本 $T_{(r)}$ 是总体分位数 $Q_p$ 的近似 $100(1-\alpha)\%$ 置信区间的构造基础（David & Nagaraja, 2003）。对 $p=0.90$、样本量 $M$，$Q_{0.90}$ 位于 $T_{(r_{\text{low}})}$ 和 $T_{(r_{\text{high}})}$ 之间的概率由二项分布给出：

$$
r_{\text{low}} = \max\!\left\{1,\ \operatorname{Binom}^{-1}(\alpha/2;\ M,\ p)\right\},
$$

$$
r_{\text{high}} = \min\!\left\{M,\ \operatorname{Binom}^{-1}(1-\alpha/2;\ M,\ p)+1\right\}.
$$

取 $\alpha=0.05$，在 $M=22\,500$ 时：

$$
r_{\text{low}} \approx 20\,118,\qquad r_{\text{high}} \approx 20\,384.
$$

由此构造

$$
Q_{0.90} \in \bigl[T_{(r_{\text{low}})},\ T_{(r_{\text{high}})}\bigr]
$$

作为 $N_{90}$ 的 $95\%$ 分布无关置信区间。若区间两端对应的体积分数舍入到两位小数后相同，则两位小数答案在该置信水平下得到认证。

### 13.2 发现—验证分离与舍入档认证

次序统计量区间未直接利用"两位小数舍入"这一离散化结构。作为补充，采用独立的发现-验证分支：

1. **发现阶段**（前 $2500$ 次试验）：冻结经验 $90\%$ 分位数对应的两位小数舍入值 $c\%$；
2. **舍入档映射**：$c\%$ 对应一个整数区间 $[N_{\text{low}}, N_{\text{high}}]$，其中所有 $N$ 的体积分数舍入后均显示为 $c\%$；
3. **验证阶段**（后 $20\,000$ 次试验，与发现样本完全独立）：
   - **下边界外侧检验**：对 $N_{\text{low}}-1$ 根圆柱，其导通概率的 $97.5\%$ 单侧 Clopper–Pearson 上界 $<0.90$，确认该档确实不满足目标；
   - **上边界达标检验**：对 $N_{\text{high}}$ 根圆柱，其导通概率的 $97.5\%$ 单侧 Clopper–Pearson 下界 $\ge 0.90$，确认该舍入档已达标。

两个检验各使用 $\alpha=0.025$ 的单侧 Clopper–Pearson 界，经 Bonferroni 校正后联合置信水平至少为 $95\%$。

Clopper–Pearson 单侧界的公式为：

$$
\operatorname{CP}_{\text{lower}}(K,M,\alpha)
= \begin{cases}
0, & K=0,\\[4pt]
\operatorname{Beta}^{-1}(\alpha;\ K,\ M-K+1), & K>0,
\end{cases}
$$

$$
\operatorname{CP}_{\text{upper}}(K,M,\alpha)
= \begin{cases}
1, & K=M,\\[4pt]
\operatorname{Beta}^{-1}(1-\alpha;\ K+1,\ M-K), & K<M.
\end{cases}
$$

## 14. 正式蒙特卡洛配置

正式计算在问题二基础上扩展，关键参数如下：

| 参数 | 取值 | 说明 |
|---|---|---|
| `BASE_SEED` | $20260808$ | 主种子，与问题二相同 |
| `PRODUCTION_TRIALS` | $22\,500$ | 正式试验次数 |
| `N_JOBS` | $10$ | 并行工作进程数 |
| `BATCH_SIZE` | $100$ | 每批次试验数（断点续算用） |
| `N_MAX` | $707$ | 搜索上界 |
| `NO_HIT_SENTINEL` | $708$ | 右删失哨兵 |

前 $2500$ 次试验的随机流与问题二的 $2500$ 次试验**完全相同**。该设计经两重回归验证：

1. **导通布尔值逐比特一致**：由 $T$ 重构的四档布尔向量与问题二缓存的 `indicators` 完全一致（SHA-256 验证通过）；
2. **稳定几何计数一致**：碎片数、AABB候选数、轴段候选数、侧面见证数、完整/裁剪 GJK 调用数、临界加密数等七项计数与前 $2500$ 次完全一致。SLSQP 的投影/重试次数因受进程级数值路径影响，不以逐项相等要求。

## 15. 问题三计算结果

### 15.1 核心结果

正式计算完成 $M=22\,500$ 次独立首次导通试验，耗时约 $3.35$ 小时。

| 指标 | 数值 |
|---|---|
| **经验 $\widehat N_{90}$** | **610** |
| **实际体积分数 $\hat\phi_{90}$** | **0.862367%** |
| **两位小数舍入答案** | **0.86%** |
| 右删失样本数 | $138$（$0.61\%$） |
| 临界加密次数 | $0$ |

### 15.2 95% 分布无关置信区间

| 项目 | 数值 |
|---|---|
| $N_{90}$ 所在区间 | $[608,\ 612]$ |
| 体积分数区间 | $[0.859540\%,\ 0.865195\%]$ |
| 区间下限舍入 | $0.86\%$ |
| 区间上限舍入 | $0.87\%$ |

区间两端舍入至两位小数后不一致，因此仅凭次序统计量在 95% 置信水平下尚不能将答案唯一确定为 $0.86\%$。

### 15.3 独立发现—验证结果

| 阶段 | 结果 |
|---|---|
| 发现样本 $N_{90}$（前 2500 次） | $609 \to 0.86\%$ |
| 候选舍入档 | $N\in[605,\ 611]$ |
| 前一档 $N=604$ | 验证成功率 $17\,734/20\,000=0.8867$，CP 上界 $=0.8911<0.90$ ✅ |
| 候选档 $N\le 611$ | 验证成功率 $18\,080/20\,000=0.9040$，CP 下界 $=0.8998\ngeq 0.90$ ❌ |

下边界外侧检验通过——$N=604$ 确实不满足 90% 目标。但上边界达标检验中，$N=611$ 的 CP 下界 $0.8998$ 略低于 $0.90$（仅差约 $0.00017$），**联合 95% 认证未通过**。

### 15.4 统计认证边界分析

联合认证未通过的原因在于 Clopper–Pearson 区间的保守性。$N=611$ 时验证样本导通比例为 $0.9040$，实质上已超过 $0.90$，但 Clopper–Pearson 方法为有限样本下的精确覆盖而采用了偏保守的下界。

该边界状态不影响点估计的可靠性。三个独立线索——全样本次序统计量、前 2500 次发现样本、后 20000 次验证样本——一致指向 $0.86\%$。

### 15.5 收敛性分析

经验 $90\%$ 分位数估计随累计试验次数的变化如下：在约 $5000$ 次试验后估计已趋于稳定，$5000$ 次以后的波动范围不超过 $\pm 1$ 根圆柱（对应体积分数波动约 $\pm 0.0014\%$），显示 $22\,500$ 次试验已充分收敛。

### 15.6 导通概率曲线

对全部 $22\,500$ 次试验，逐 $N$ 计算经验导通概率 $\hat P_N = M^{-1}\sum_{m=1}^{M}\mathbf 1\{T_m\le N\}$。曲线严格单调上升，在 $N\approx 550$ 至 $N\approx 650$ 区间从约 $0.10$ 快速攀升至约 $0.98$。$N=610$ 处经验概率为 $0.9004$，$N=707$ 处达到 $0.9939$。

## 16. 数值稳定性

全部 $22\,500$ 次试验的 `near_threshold` 总计数为 **0**，表明没有任何一对碎片的 GJK 距离落入 $1.8$ nm 附近的 $10^{-4}$ nm 临界带。与问题二的 $2500$ 次试验一致，本次求解的数值几何管线保持稳定。

## 17. 模型局限与论文表述边界

1. **概率模型假设**：本模型的导通概率严格依赖于 i.i.d. 均匀位置 + 各向同性方向的假设。若实际填充工艺下的介质分布偏离该理想分布，概率估计需要重新校准；
2. **统计认证的边界状态**：$22\,500$ 次试验下联合 95% 认证未通过，差约 $0.00017$。若竞赛要求严格的置信认证，可将样本量扩大至 $30\,000$ 以上，或如实报告 CP 保守性导致的认证边界；
3. **体积分数的线性近似**：每根圆柱均为完整 $5000$ nm 长，不考虑末端被边界截断导致的体积损失。由于被截断的圆柱数量占比很小（截面面积 $\pi r^2\approx 2827$ nm$^2$，与盒面 $10000^2$ nm$^2$ 相比可忽略），这种简化引入的系统误差远小于蒙特卡洛的统计误差；
4. **允许重叠的体积影响**：在极高填充量下，圆柱间体积重叠不可忽略，但本文体积分数始终使用未扣除重叠的名义值，以与题目表述方式保持一致；
5. **所有计算基于双精度浮点数**，几何求解使用了 SLSQP 局部优化和 Dykstra 投影，在极端退化构型下存在极小概率的数值失败可能（本次 $22\,500$ 次运行中未出现）。

## 18. 本问结论

**问题三的两位小数答案为 0.86%，对应 $N_{90}=610$ 根介质 A 圆柱，经验导通概率为 $0.9004$。**

该答案得到三个方面的一致支持：全样本次序统计量点估计、前 2500 次发现样本的经验分位数、后 20000 次验证样本的区间排除。在现有 $22\,500$ 次蒙特卡洛试验下，$0.86\%$ 是导通概率不低于 $90\%$ 的最低两位小数体积分数。

---

## 附录：算法与数据结构总览

三个问题的求解共享以下核心组件：

| 组件 | 问题一 | 问题二 | 问题三 |
|---|---|---|---|
| 圆柱几何表示 | `Cylinder`（解析支持函数） | 同左 | 同左 |
| 裁剪片段表示 | `ExactClippedCylinder`（精确交集体） | `Fragment`（周期裁剪碎片） | 同问题二 |
| 距离算法 | Signed Volumes GJK | `closest_simplex` GJK | 同问题二 |
| 导通判定 | 并查集 + BFS | 并查集 | 同左 |
| 随机模型 | 无（确定性附件） | PCG64 + SeedSequence | 同左 |
| 关键差异 | 逐行独立节点，无周期副本 | i.i.d. 随机 + 周期裁剪 | 首次导通全档位搜索 |
