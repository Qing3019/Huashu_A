# 模型评价

## 一、模型优点

**几何建模方面**，本模型将导电介质片段严格表示为有限圆柱（或球体）与正方体的交集。支持点由解析约束或 SLSQP 原问题直接求解；边界截断处由平面自然切出而非强行构造圆形端盖，附件数据每行与图节点一一对应，不引入未经证实的同源合并。四问共用同一套 Fragment—GJK—并查集几何引擎，Q3 前 $2500$ 次试验的导通布尔值与 Q2 缓存经 SHA-256 验证逐比特一致，跨问题几何自洽性得到充分保障。全部 GJK 计算均收敛，$1.8$ nm 阈值附近 `near_threshold` 统计量为零，导通判定在数值上稳健、在审计上可追溯。

**统计设计方面**，Q3 和 Q4 均采用"发现—冻结—验证"的预注册框架。发现阶段用一批样本寻优并冻结候选，冻结规则在接触验证数据之前预先固定；验证阶段用完全独立的新样本仅检验该候选，不根据验证结果回头调整方案。这一设计从机制上杜绝了 data snooping——同一批随机波动既参与选优又参与评价的循环论证。Q3 同时使用非参数次序统计量区间和 Clopper–Pearson 精确区间进行双重认证：前者不依赖任何分布假设，直接由首次导通时间 $T$ 的排序位置构造 $N_{90}$ 的 $95\%$ 置信区间，得到 $[608,611]$，两端舍入均为 $0.86\%$；后者在有限样本下严格保证覆盖率，候选舍入档的单侧 CP 下界 $0.9001>0.90$。两种独立方法结论一致，互相印证。Q4 以"发现样本率 $\ge 91\%$"的稳健筛选规则冻结候选 $(611,25)$，在独立验证中导通 $9066/10000$，单侧 CP 下界 $0.9017>0.90$。认证方案成本仅比点估计最优高出 $0.86\%$——以微小的经济代价换取了可量化、可复核的统计可靠性。全部试验采用确定性 `SeedSequence` 初始化 PCG64 随机流，使结果在任何机器和并行调度下均可精确复现。

**计算效率方面**，导通事件对介质数量的严格单调性被充分利用：Q2 和 Q3 的一次嵌套试验同时服务于多个圆柱数量档位，Q4 的一次建图通过 $(a_{\text{req}},b_{\text{req}})$ 边标签机制回答所有 $(N_A,N_B)$ 组合的导通查询，大幅减少了重复的几何计算。AABB—外包圆柱 GJK—精确片段 GJK 的三级筛选策略则使绝大多数远距离片段对被安全排除在昂贵的精确计算之外，在保证几何精度的同时控制了计算成本。

## 二、模型局限

**概率假设方面**，导通概率建立在介质位置 i.i.d. 均匀、方向各向同性的理想假设上。题目仅说明介质可处于"任意位置、任意方向"，本文的 i.i.d. 假设是对此最自然的数学化处理，但实际填充工艺可能导致介质聚集或定向排列，使概率估计产生偏差。此外，模型允许介质相互贯穿重叠，体积分数使用名义值而非扣除重叠的有效值——在本文约 $0.86\%\sim0.88\%$ 的低填充区间内重叠概率极低，此简化引入的误差远小于蒙特卡洛统计误差；若推广至更高填充量场景，需补充重叠修正。

**物理简化方面**，模型以 $1.8$ nm 最短几何距离作为导通/不导通的硬阈值，未纳入介质表面粗糙度对有效接触距离的影响、亚纳米间隙中的量子隧穿效应以及介质材料电阻率差异等微观物理因素。几何连通是导电的必要条件而非充分条件，这一简化在题目给定的信息范围内合理且必要。
